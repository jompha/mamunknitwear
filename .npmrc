# eslint-plugin-jsx-a11y@6.x still peers only with eslint <=9, while this
# project uses ESLint 10 flat config (the plugin works fine with it). Ignore
# that one peer conflict for both `npm install` and `npm ci`.
legacy-peer-deps=true
