---
import { site, sisterConcerns, navigation } from '../../data/site';
import { withBase } from '../../utils/paths';
import Icon from '../common/Icon.astro';
import { getCollection } from 'astro:content';

const recentNews = (await getCollection('news'))
  .filter((p) => !p.data.draft)
  .sort((a, b) => b.data.pubDate.getTime() - a.data.pubDate.getTime())
  .slice(0, 3);
const year = new Date().getFullYear();
---

<footer class="site-footer">
  <div class="site-footer__brandband">
    <div class="container site-footer__brand">
      <div>
        <span class="eyebrow eyebrow--dark">Mamun Knitwear Ltd.</span>
        <p class="site-footer__tagline">{site.tagline}</p>
      </div>
      <div class="site-footer__cta">
        <a class="btn btn--primary" href={withBase('/contact-us/')}>
          Start a conversation
          <Icon name="arrow-right" size={16} />
        </a>
      </div>
    </div>
  </div>

  <div class="container">
    <div class="site-footer__grid">
      <div class="site-footer__col">
        <h2 class="site-footer__title">Get in touch</h2>
        <p class="site-footer__text">
          We know you want business and our experienced team knows how to do that. Come over and
          meet them at Mamun Knitwear Ltd. See for yourself and get in touch.
        </p>
        <address class="site-footer__contact">
          <p>
            <Icon name="map-pin" size={15} />
            {site.address.line1}, {site.address.line2}, Bangladesh
          </p>
          <p>
            <Icon name="phone" size={15} />
            <a href={`tel:${site.phoneRaw}`}>{site.phone}</a>
          </p>
          <p>
            <Icon name="phone" size={15} />
            <a href={`tel:${site.phoneAltRaw}`}>{site.phoneAlt}</a>
          </p>
          <p>
            <Icon name="mail" size={15} />
            <a href={`mailto:${site.publicEmail}`}>{site.publicEmail}</a>
          </p>
        </address>
      </div>

      <nav class="site-footer__col" aria-label="Footer navigation">
        <h2 class="site-footer__title">Navigation</h2>
        <ul class="site-footer__links">
          {
            navigation.map((item) => (
              <li>
                <a href={withBase(item.href)}>{item.label}</a>
                {item.children && (
                  <ul class="site-footer__sublinks">
                    {item.children.map((child) => (
                      <li>
                        <a href={withBase(child.href)}>{child.label}</a>
                      </li>
                    ))}
                  </ul>
                )}
              </li>
            ))
          }
        </ul>
      </nav>

      <div class="site-footer__col">
        <h2 class="site-footer__title">Sister Concerns</h2>
        <p class="site-footer__text">Part of a family of specialist companies:</p>
        <ul class="site-footer__links">
          {
            sisterConcerns.map((s) => (
              <li>
                <a href={withBase('/about-us-2/')}>{s.name}</a>
              </li>
            ))
          }
        </ul>
      </div>

      <div class="site-footer__col">
        <h2 class="site-footer__title">Latest News</h2>
        <ul class="site-footer__links site-footer__news">
          {
            recentNews.map((post) => (
              <li>
                <a href={withBase(`/${post.id}/`)}>{post.data.title}</a>
                <time datetime={post.data.pubDate.toISOString().slice(0, 10)}>
                  {post.data.pubDate.toLocaleDateString('en-US', {
                    year: 'numeric',
                    month: 'long',
                    day: 'numeric',
                  })}
                </time>
              </li>
            ))
          }
        </ul>
      </div>
    </div>
  </div>

  <div class="copyright">
    <div class="container copyright__inner">
      <p>
        © {year}
        <a href={site.website} target="_blank" rel="noopener noreferrer">MamunKnitwear.com</a>. All
        rights reserved.
      </p>
      <p>
        Static rebuild by <a href={`mailto:${site.ownerEmail}`}>{site.owner}</a> ({
          site.ownerTitle
        }).
      </p>
    </div>
  </div>
</footer>

<style>
  .site-footer {
    background: var(--color-footer-bg);
    color: var(--color-footer-text);
  }

  .site-footer__brandband {
    border-bottom: 1px solid rgb(255 255 255 / 0.08);
  }

  .site-footer__brand {
    display: flex;
    align-items: center;
    justify-content: space-between;
    gap: var(--space-5);
    flex-wrap: wrap;
    padding-block: var(--space-8);
  }

  .site-footer__tagline {
    color: var(--color-footer-heading);
    font-family: var(--font-heading);
    font-size: var(--fs-2xl);
    font-weight: var(--fw-semibold);
    letter-spacing: -0.01em;
    max-width: 46ch;
  }

  .site-footer__grid {
    display: grid;
    gap: var(--space-8);
    padding-block: var(--space-8) var(--space-10);
  }

  @media (min-width: 768px) {
    .site-footer__grid {
      grid-template-columns: repeat(2, 1fr);
    }
  }

  @media (min-width: 1024px) {
    .site-footer__grid {
      grid-template-columns: repeat(4, 1fr);
    }
  }

  .site-footer__title {
    color: var(--color-footer-heading);
    font-size: var(--fs-sm);
    font-weight: var(--fw-semibold);
    text-transform: uppercase;
    letter-spacing: 0.1em;
    margin-bottom: var(--space-4);
    padding-bottom: var(--space-3);
    border-bottom: 2px solid var(--color-accent-bright);
    display: inline-block;
  }

  .site-footer__text {
    color: var(--color-footer-text);
    margin-bottom: var(--space-4);
    font-size: var(--fs-sm);
    line-height: var(--lh-body);
  }

  .site-footer__contact p {
    display: flex;
    align-items: flex-start;
    gap: var(--space-2);
    margin-bottom: var(--space-3);
    font-size: var(--fs-sm);
    color: var(--color-footer-text);
    font-style: normal;
  }

  .site-footer__contact svg {
    flex-shrink: 0;
    margin-top: 3px;
    color: var(--color-accent-bright);
  }

  .site-footer a {
    color: var(--color-footer-text);
    text-decoration: none;
    transition: color var(--transition);
  }

  .site-footer a:hover,
  .site-footer a:focus-visible {
    color: var(--color-accent-bright);
    text-decoration: underline;
    text-underline-offset: 3px;
  }

  .site-footer__links {
    list-style: none;
    padding: 0;
  }

  .site-footer__links li {
    margin-bottom: var(--space-2);
    font-size: var(--fs-sm);
  }

  .site-footer__sublinks {
    list-style: none;
    padding: 0;
    margin: var(--space-1) 0 0 var(--space-4);
  }

  .site-footer__sublinks li {
    margin-bottom: var(--space-1);
    font-size: var(--fs-xs);
  }

  .site-footer__sublinks a {
    color: var(--color-footer-muted);
  }

  .site-footer__news time {
    display: block;
    color: var(--color-footer-muted);
    font-size: var(--fs-xs);
    margin-top: 2px;
  }

  .copyright {
    border-top: 1px solid rgb(255 255 255 / 0.08);
    background: #0f0d0b;
    color: var(--color-copyright);
    font-size: var(--fs-sm);
  }

  .copyright__inner {
    display: flex;
    flex-wrap: wrap;
    justify-content: space-between;
    gap: var(--space-2);
    padding-block: var(--space-4);
  }

  .copyright a {
    color: var(--color-copyright);
  }

  .copyright a:hover,
  .copyright a:focus-visible {
    color: var(--color-accent-bright);
  }
</style>
