---
import BaseLayout from '../layouts/BaseLayout.astro';
import Breadcrumbs from '../components/layout/Breadcrumbs.astro';
import PageHeader from '../components/sections/PageHeader.astro';
import CtaSection from '../components/sections/CtaSection.astro';
import Icon from '../components/common/Icon.astro';
import { services } from '../data/services';
import { site } from '../data/site';
import svcKnitting from '../assets/images/svc-knitting.jpg';

const jsonLd = [
  {
    '@context': 'https://schema.org',
    '@type': 'ItemList',
    name: `${site.name} services`,
    itemListElement: services.map((service, index) => ({
      '@type': 'ListItem',
      position: index + 1,
      name: service.title,
    })),
  },
];
---

<BaseLayout
  title="Services"
  description="Explore the services of Mamun Knitwear Ltd. â€” knitting, sample, cutting, sewing, finishing and embroidery."
  canonicalPath="/services/"
  jsonLd={jsonLd}
>
  <Breadcrumbs items={[{ label: 'Services' }]} />
  <PageHeader
    title="Our services"
    eyebrow="What we do"
    lead="Integrated manufacturing facilities for export quality knit garments."
    image={svcKnitting}
    imageAlt="Knitting machines at Mamun Knitwear Ltd."
  />

  <section class="section">
    <div class="container">
      <ul class="services-list">
        {
          services.map((service, index) => (
            <li class="service-row">
              <a href={service.href} class="service-row__card">
                <div class="service-row__media">
                  <img
                    src={service.image.src}
                    alt={service.imageAlt}
                    width={service.image.width}
                    height={service.image.height}
                    loading="lazy"
                  />
                </div>
                <div class="service-row__body">
                  <span class="service-row__index" aria-hidden="true">
                    {String(index + 1).padStart(2, '0')}
                  </span>
                  <h2>{service.title}</h2>
                  <p>{service.summary}</p>
                  <span class="service-row__link">
                    Read more
                    <Icon name="arrow-right" size={15} />
                  </span>
                </div>
              </a>
            </li>
          ))
        }
      </ul>
    </div>
  </section>

  <CtaSection
    title="Ready to work with us?"
    text="Contact us to discuss your next order with our manufacturing team."
    ctaLabel="Contact us"
    ctaHref="/contact-us/"
  />
</BaseLayout>

<style>
  .services-list {
    list-style: none;
    padding: 0;
    display: grid;
    gap: var(--space-6);
  }

  .service-row__card {
    display: grid;
    grid-template-columns: 340px 1fr;
    gap: 0;
    align-items: stretch;
    background: var(--color-white);
    border: 1px solid var(--color-border);
    border-radius: var(--radius-lg);
    overflow: hidden;
    text-decoration: none;
    transition:
      transform var(--transition-slow),
      box-shadow var(--transition-slow),
      border-color var(--transition-slow);
  }

  .service-row__card:hover,
  .service-row__card:focus-visible {
    transform: translateY(-4px);
    box-shadow: var(--shadow-md);
    border-color: rgb(2 132 199 / 0.25);
  }

  @media (max-width: 860px) {
    .service-row__card {
      grid-template-columns: 1fr;
    }
  }

  .service-row__media {
    overflow: hidden;
  }

  .service-row__media img {
    width: 100%;
    height: 100%;
    aspect-ratio: 16 / 10;
    object-fit: cover;
    transition: transform var(--transition-slow);
  }

  .service-row__card:hover .service-row__media img {
    transform: scale(1.04);
  }

  @media (min-width: 861px) {
    .service-row__media img {
      aspect-ratio: auto;
      height: 100%;
    }
  }

  .service-row__body {
    padding: var(--space-7);
    display: flex;
    flex-direction: column;
    gap: var(--space-3);
    align-items: center;
    text-align: center;
    position: relative;
  }

  .service-row__index {
    font-family: var(--font-heading);
    font-size: var(--fs-sm);
    font-weight: var(--fw-bold);
    color: var(--color-accent);
    letter-spacing: 0.1em;
  }

  .service-row__body h2 {
    color: var(--color-heading);
    font-size: var(--fs-xl);
  }

  .service-row__body p {
    font-size: var(--fs-sm);
    flex: 1;
  }

  .service-row__link {
    display: inline-flex;
    align-items: center;
    gap: var(--space-1);
    color: var(--color-accent);
    font-weight: var(--fw-semibold);
    font-size: var(--fs-sm);
  }

  .service-row__link svg {
    transition: transform var(--transition);
  }

  .service-row__card:hover .service-row__link svg {
    transform: translateX(4px);
  }
</style>
